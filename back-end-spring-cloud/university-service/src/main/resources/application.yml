# 文件说明：拾光记微服务后端大学服务配置文件，统一维护服务端口、注册发现、数据源和中间件参数。
spring:
  application:
    name: high-service
  datasource:
    dynamic:
      primary: cot_university
      strict: false
      datasource:
        cot_university:
          url: jdbc:mysql://localhost:3306/cot_university?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
          username: root
          #password: 0924
          password: Liu0924#
          driver-class-name: com.mysql.cj.jdbc.Driver
        cot_university_legacy:
          url: jdbc:mysql://localhost:3306/cot_university?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowMultiQueries=true
          username: root
          #password: 0924
          password: Liu0924#
          driver-class-name: com.mysql.cj.jdbc.Driver

server:
  port: 8083

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true